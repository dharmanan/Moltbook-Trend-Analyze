# MoltBridge Agent — Reply Cycle
# Scheduled daily at 11:00 Turkey time (08:00 UTC) with an every-other-day gate, one hour after hot post.

name: 🦞 MoltBridge Reply

on:
  schedule:
    - cron: '0 8 * * *'
  workflow_dispatch:

jobs:
  reply:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      SCHEDULE_ANCHOR_DATE: '2026-03-12'

    steps:
      - name: 🗓️ Schedule gate
        id: schedule_gate
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
            echo "reason=manual" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          today_epoch=$(date -u -d "$(date -u +%F)" +%s)
          anchor_epoch=$(date -u -d "$SCHEDULE_ANCHOR_DATE" +%s)
          days_since_anchor=$(((today_epoch - anchor_epoch) / 86400))

          if (( days_since_anchor % 2 == 0 )); then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
            echo "reason=scheduled-run-day" >> "$GITHUB_OUTPUT"
          else
            echo "should_run=false" >> "$GITHUB_OUTPUT"
            echo "reason=scheduled-skip-day" >> "$GITHUB_OUTPUT"
          fi

      - name: 📥 Checkout
        if: steps.schedule_gate.outputs.should_run == 'true'
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        if: steps.schedule_gate.outputs.should_run == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 📦 Install dependencies
        if: steps.schedule_gate.outputs.should_run == 'true'
        run: pip install -r requirements.txt

      - name: 📂 Restore data cache
        if: steps.schedule_gate.outputs.should_run == 'true'
        uses: actions/cache@v4
        with:
          path: data/state/
          key: moltbridge-data-${{ github.ref_name }}-${{ github.run_id }}
          restore-keys: |
            moltbridge-data-${{ github.ref_name }}-
            moltbridge-data-

      - name: 💬 Run reply
        if: steps.schedule_gate.outputs.should_run == 'true'
        env:
          MOLTBOOK_API_KEY: ${{ secrets.MOLTBOOK_API_KEY }}
          MOLTBOOK_AGENT_NAME: ${{ secrets.MOLTBOOK_AGENT_NAME || 'MoltBridgeAgent' }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          LOG_LEVEL: INFO
        run: python src/main.py --reply

      - name: 🤝 Follow commenters
        if: steps.schedule_gate.outputs.should_run == 'true'
        env:
          MOLTBOOK_API_KEY: ${{ secrets.MOLTBOOK_API_KEY }}
          MOLTBOOK_AGENT_NAME: ${{ secrets.MOLTBOOK_AGENT_NAME || 'MoltBridgeAgent' }}
          LOG_LEVEL: INFO
        run: python scripts/follow_agents.py --commenters

      - name: ⏭️ Skip summary
        if: steps.schedule_gate.outputs.should_run != 'true'
        run: |
          echo "## 💬 MoltBridge Reply Skipped" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Reason: alternating-day schedule gate" >> $GITHUB_STEP_SUMMARY
          echo "- Anchor date: $SCHEDULE_ANCHOR_DATE" >> $GITHUB_STEP_SUMMARY
          echo "- Time: $(date -u +%Y-%m-%dT%H:%M:%SZ)" >> $GITHUB_STEP_SUMMARY

      - name: 💾 Save data
        if: always() && steps.schedule_gate.outputs.should_run == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: moltbridge-reply-data-${{ github.run_number }}
          path: |
            data/state/
          retention-days: 30

      - name: 📊 Summary
        if: success() && steps.schedule_gate.outputs.should_run == 'true'
        run: |
          echo "## 💬 MoltBridge Reply Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Run: #${{ github.run_number }}" >> $GITHUB_STEP_SUMMARY
          echo "- Time: $(date -u +%Y-%m-%dT%H:%M:%SZ)" >> $GITHUB_STEP_SUMMARY
          echo "- Schedule: Daily 11:00 Turkey / 08:00 UTC with every-other-day gate" >> $GITHUB_STEP_SUMMARY
